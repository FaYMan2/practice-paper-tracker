/** Sizing for the accuracy chart. */

export const ROW_HEIGHT = 26;

export const BAR_SIZE = 10;

/**
 * Below this, a subject is drawn in the wrong-answer colour.
 *
 * Two thirds is not a claim about GATE marking — it is a reading aid, so the
 * subjects worth revising separate from the rest at a glance.
 */
export const WEAK_THRESHOLD = 0.67;
