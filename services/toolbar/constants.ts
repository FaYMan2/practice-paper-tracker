/** Toolbar constants. */

/**
 * Built from `entrypoints/dashboard/index.html`, which WXT emits at the root of
 * the bundle rather than in a folder.
 */
export const DASHBOARD_PAGE = "/dashboard.html";
