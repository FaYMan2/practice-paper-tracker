- Fixes: #
- Demo: <!-- neetoRecord or Loom. Screenshots are fine only if nothing here is interactive. -->
- <!-- What changed, and why. One idea per bullet. -->

---

- [ ] An issue was opened first and the approach agreed
- [ ] `pnpm test` and `pnpm compile` pass
- [ ] No new attempt is recorded by anything that only *displays* progress

<!--
The last box is not boilerplate. The capture observer watches the site's own
`mtq_correct_stamp` / `mtq_wrong_stamp` classes, so anything that paints those
logs an answer the user never gave. See "Before you start" in the README.
-->
